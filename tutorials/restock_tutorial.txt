SARI-SARI SMART (ANDROID) — RESTOCK TUTORIAL (8 STEPS)
======================================================
Config: TutorialOverlay.kt -> pageTutorials ("restock", prefix "restockTutorial")
Page: restock (all steps)
Launch: Restock Day page header (?) button -> in-place launch (startPageTutorial("restock"));
        Settings -> Tutorial selector / Help -> Tutorial selector (navigates here if needed)

NOTE ON TRANSLATION: The `restockTutorial1..8` keys exist ONLY in the English
(en) dictionary of Strings.kt — there is NO Filipino (fil) translation. When
the app language is Filipino, the `t()` helper falls back to English
(dict[key] ?: en[key] ?: key). All 8 steps therefore show the English text
below in both languages. This matches the web app's documented behavior.

SHARED STEP FIELDS
- Highlight appearance: pure outline — transparent center (element stays visible), white 2dp inner ring +
  green 4dp outer ring (offset 2dp outward), rounded corners ~12-14dp, -4dp offset around the target
- Navigation: Previous appears from step 2 onward; Next advances; on the last step the button reads
  "Get Started" and finishes in place; Skip (left side) + backdrop tap dismiss only on replay
- Box position rule: no highlight -> bottom center; target center in top 45% of screen -> card at bottom;
  otherwise card at top (below the header pill)
- Auto-scroll: when the step's highlight target is off-screen, the page scrolls to reveal it (where the
  screen provides a scroll state)

----------------------------------------------------------------------
STEP 1 — Intro
  Key      : restockTutorial1
  Text EN : "This is the Restock Day page - a guided 2-step workflow to update inventory."
  Text FIL: (none — falls back to English, see note above)
  Target element : none
  Highlight      : none
  Box position   : bottom center
  Interaction    : read text, tap Next
----------------------------------------------------------------------
STEP 2 — Step 1: shelf check
  Key      : restockTutorial2
  Text EN : "Step 1: Check your shelves. Update the actual count for each product."
  Text FIL: (none — falls back to English, see note above)
  Target element : restockStep1Section (RestockScreen)
  Highlight      : the Step 1 section header (top of page)
  Box position   : expected bottom
  Interaction    : look at step 1, tap Next
----------------------------------------------------------------------
STEP 3 — Search / actual count
  Key      : restockTutorial3
  Text EN : "Enter the real count you see on your shelf next to what the app thinks."
  Text FIL: (none — falls back to English, see note above)
  Target element : restockSearchField (RestockScreen)
  Highlight      : the search / count-entry box
  Box position   : expected bottom
  Interaction    : look at the count entry, tap Next
----------------------------------------------------------------------
STEP 4 — Product list
  Key      : restockTutorial4
  Text EN : "Green check = count matches. Red = unrecorded sales were found."
  Text FIL: (none — falls back to English, see note above)
  Target element : restockProductList (RestockScreen)
  Highlight      : the product list with status icons
  Box position   : per runtime rule
  Interaction    : look at the product list, tap Next
----------------------------------------------------------------------
STEP 5 — Continue to Purchases
  Key      : restockTutorial5
  Text EN : "After checking, tap Continue to Purchases to record what you bought."
  Text FIL: (none — falls back to English, see note above)
  Target element : restockContinueBtn (RestockScreen)
  Highlight      : the Continue to Purchases button
  Box position   : per runtime rule
  Interaction    : see the Continue button, tap Next
----------------------------------------------------------------------
STEP 6 — Step 2: purchases
  Key      : restockTutorial6
  Text EN : "Step 2: Search for a product, enter cost per unit and quantity, tap Add Item."
  Text FIL: (none — falls back to English, see note above)
  Target element : restockStep2Section (RestockScreen)
  Highlight      : the Step 2 section header
  Box position   : per runtime rule
  Interaction    : look at the product entry, tap Next
----------------------------------------------------------------------
STEP 7 — Purchase list
  Key      : restockTutorial7
  Text EN : "Review your purchases. Total cost is shown at the bottom."
  Text FIL: (none — falls back to English, see note above)
  Target element : restockPurchaseList (RestockScreen)
  Highlight      : the purchase list
  Box position   : per runtime rule
  Interaction    : look at the purchase list, tap Next
----------------------------------------------------------------------
STEP 8 — Done
  Key      : restockTutorial8
  Text EN : "Tap Done to save. Your inventory is updated. Find the reminder on Morning page."
  Text FIL: (none — falls back to English, see note above)
  Target element : restockDoneBtn (RestockScreen)
  Highlight      : the Done button (bottom of page)
  Box position   : expected top
  Interaction    : see the button, tap Get Started (page tutorial finishes in place)
----------------------------------------------------------------------
