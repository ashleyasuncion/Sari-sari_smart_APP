SARI-SARI SMART (ANDROID) — REPORTS TUTORIAL (6 STEPS)
======================================================
Config: TutorialOverlay.kt -> pageTutorials ("report", prefix "reportTutorial")
Page: reports (all steps — the tutorial now plays on the Reports screen itself;
      previously it was anchored to "morning" and launched from Home)
Launch: Reports page header (?) button -> in-place launch (startPageTutorial("report"));
        Settings -> Tutorial selector / Help -> Tutorial selector (navigates to the
        Reports page first if you aren't already there)

SHARED STEP FIELDS
- Highlight appearance: pure outline — transparent center (element stays visible), white 2dp inner ring +
  green 4dp outer ring (offset 2dp outward), rounded corners ~12-14dp, -4dp offset around the target
- Navigation: Previous appears from step 2 onward; Next advances; on the last step the button reads
  "Get Started" and finishes in place; Skip (left side) + backdrop tap dismiss only on replay
- Box position rule: no highlight -> bottom center; target center in top 45% of screen -> card at bottom;
  otherwise card at top (below the header pill)
- Auto-scroll: when the step's highlight target is off-screen, the page scrolls to reveal it (where the
  screen provides a scroll state)

STEP 1 — Reports intro
  Key      : reportTutorial1
  Text EN : "The Reports page shows your store's sales performance over time."
  Text FIL: "Ang Reports page ay nagpapakita ng performance ng iyong tindahan sa paglipas ng panahon."
  Target element : none
  Highlight      : none
  Box position   : bottom center
  Interaction    : read text, tap Next
STEP 2 — Period toggle
  Key      : reportTutorial2
  Text EN : "Use the toggle buttons to switch between Daily, Weekly, and Monthly views."
  Text FIL: "Gamitin ang toggle buttons para lumipat sa Daily, Weekly, o Monthly views."
  Target element : reportPeriodToggle (ReportsScreen)
  Highlight      : the period toggle (top of page)
  Box position   : expected bottom
  Interaction    : look at the toggles, tap Next
STEP 3 — Summary cards
  Key      : reportTutorial3
  Text EN : "The summary cards show total sales and profit for the selected period."
  Text FIL: "Ang summary cards ay nagpapakita ng total sales at profit para sa napiling panahon."
  Target element : reportSummaryCards (ReportsScreen)
  Highlight      : the summary cards section
  Box position   : per runtime rule
  Interaction    : look at the cards, tap Next
STEP 4 — Recent transactions
  Key      : reportTutorial4
  Text EN : "Recent transactions appear here with details on each sale. Scroll to see more."
  Text FIL: "Ang mga recent transactions ay lumalabas dito na may detalye ng bawat benta."
  Target element : reportRecentTx (ReportsScreen)
  Highlight      : the recent-transactions section
  Box position   : per runtime rule
  Interaction    : look at the section, tap Next
STEP 5 — Best-Selling Products
  Key      : reportTutorial5
  Text EN : "The Best-Selling Products section shows your top-performing items."
  Text FIL: "Ang Best-Selling Products section ay nagpapakita ng iyong mga top-performing items."
  Target element : reportBestSellers (ReportsScreen)
  Highlight      : the best-selling products section
  Box position   : per runtime rule
  Interaction    : look at the section, tap Next
STEP 6 — Low-stock items
  Key      : reportTutorial6
  Text EN : "Low-stock items are listed here so you know what needs restocking."
  Text FIL: "Ang low-stock items ay nakalista dito para malaman mo kung ano ang kailangan i-restock."
  Target element : reportLowStock (ReportsScreen)
  Highlight      : the low-stock items section
  Box position   : per runtime rule
  Interaction    : look at the section, tap Get Started (page tutorial finishes in place)
